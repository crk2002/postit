<% if obj.errors.any? %>
  <div class="alert alert-error">
    <p>Your submission had <%= pluralize(obj.errors.size, "error")%>:</p>
    <ul>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %> </li>
    <% end %>
    </ul>
  </div>
<% end %>